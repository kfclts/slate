### Access Permission
Role | Scope
---- | -----
Root | Forbidden.
Admin | Full permission, can careate data which <code>companyId</code> same as admin's, and can modify <code>userId</code> for a specific user.
User  | Creates data which <code>userId</code> same as user's.
